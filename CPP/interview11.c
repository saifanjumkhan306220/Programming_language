class player
{
        private: player()
        {
        }
        public : player(int x)
        {
            score =x;
        }
};

/*Answer:
a. The object can be created without parameters
b. Only the object with only 1 parameter can be created
c. The object can never be created
d. Only the object with some parameters can be created
*/